
export function sendEmail(to: string, subject: string, text: string) {
    // Implement your email sending logic here, e.g., using nodemailer or any email service API
    console.log(`Sending email to ${to} with subject "${subject}" and body: ${text}`);
}