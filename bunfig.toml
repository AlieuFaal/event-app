# Bun configuration for monorepo
[install]
# Use exact versions for better reproducibility
exact = true

# Only resolve package versions published at least 3 days ago.
# This reduces exposure to freshly published supply-chain compromises.
minimumReleaseAge = 259200

# Auto-install peer dependencies
auto = true

# Freeze lockfile in CI
frozen-lockfile = false

# Hoist dependencies to root node_modules
# This ensures all workspaces use the same versions
[install.scopes]
"@vibespot" = { }

# Workspace configuration
[install.workspace]
# Enable workspace protocol support
enabled = true
